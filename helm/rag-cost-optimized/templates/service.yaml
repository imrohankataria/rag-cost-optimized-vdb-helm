apiVersion: v1
kind: Service
metadata:
  name: {{ include "rag-cost-optimized.fullname" . }}
  labels:
    {{- include "rag-cost-optimized.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rag-cost-optimized.selectorLabels" . | nindent 4 }}
